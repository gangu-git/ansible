---
 - hosts: all
   become: yes
   tasks:
    - name: installing apache on ubuntu node
      apt:
       name: apache2
       state: present
      when: ansible_os_family=="Debian"
    - name: installing httpd on redhat node 
      yum:
       name: httpd
       state: present
      when: ansible_os_family=="RedHat"
